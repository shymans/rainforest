<h1>Products</h1>
<%= link_to "New Product", "/products/new" %>

<% @products.each do |product| %>
  <p><%= product[:name] %>, <%= product[:description] %>, <%= product.formatted_price %>
  <%= link_to "Item Page", "/products/#{product.id}" %></p>
<% end %>

